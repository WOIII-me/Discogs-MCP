{
  "name": "@woiii-me/discogs-mcp",
  "version": "1.5.2",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "dev:token": "wrangler dev src/dev-server.ts --port 8787",
    "build": "wrangler deploy --dry-run",
    "deploy": "wrangler deploy",
    "deploy:prod": "wrangler deploy --env production",
    "test": "vitest run",
    "test:watch": "vitest",
    "package:ext": "node scripts/package-extension.mjs",
    "validate:submission": "node scripts/validate-openai-submission.mjs",
    "check:oauth": "node scripts/check-openai-oauth-contract.mjs",
    "check:surfaces": "node scripts/check-openai-public-surfaces.mjs",
    "inspect": "npx @modelcontextprotocol/inspector http://localhost:8787/mcp",
    "lint": "tsc --noEmit",
    "setup:kv": "wrangler kv namespace create OAUTH_KV && wrangler kv namespace create CACHE_KV"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260902.1",
    "@types/node": "^26.4.1",
    "typescript": "^7.0.2",
    "vitest": "^4.1.11",
    "wrangler": "^4.128.0"
  },
  "dependencies": {
    "@cloudflare/workers-oauth-provider": "^0.10.3",
    "@modelcontextprotocol/client": "^2.0.0",
    "@modelcontextprotocol/sdk": "^1.30.0",
    "@modelcontextprotocol/server": "^2.0.0",
    "agents": "^0.20.1",
    "oauth-1.0a": "^2.2.6",
    "zod": "^4.5.4"
  },
  "overrides": {
    "@hono/node-server": ">=2.0.5"
  }
}
